// const urls = [treeUrl];

// Promise.all(urls.map(url => d3.json(url))).then(run);

// function run(dataset) {
//    runFunction(dataset[0]);
//    // d3PieChart(dataset[0], dataset[1]);
//    // d3BarChart(dataset[1]);
//    // runFunction(dataset[2]);
// };